docker run -td -p 80(host):80(container) --name  container-name   image-name //bin/bash
           container port always by deafault port
docker run -td  -p  80:80 --name  container1   ubuntu //bin/bash           


           
           
